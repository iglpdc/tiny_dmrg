#!/bin/bash
svn up
rev=$(svnversion -n)
TMPDIR=./dmrgloo
TARBALL=./dmrgloo.tar.gz
svn export `svn info | grep URL: | awk '{print $2}'` $TMPDIR
tar -cvzf $TARBALL $TMPDIR
rm -rf $TMPDIR
echo 'Revision '$rev' exported to '$TARBALL' file'
